export default class Series{

      data:Array<any> = []
     constructor(data:any, index:Array<any>, type:string){
     // console.log(data)
     // console.log(index)
     // console.log(type)
     this.data = data
     }

     // get data_(){
     //      return JSON.parse(JSON.stringify(this.data))
     // }
    
}